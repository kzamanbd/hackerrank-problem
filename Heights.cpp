#include <bits/stdc++.h>
using namespace std;

int main()
{

    int n;
    cin >> n;
    int heights[n], leftMax[n], rightMax[n];

    for (int i = 0; i < n; i++)
    {
        cin >> heights[i];
    }

    return 0;
}